# NOTE: This file is auto-generated by tooling/generate-test-scripts.py

name: TestMT1993732 (AMD64 GNU/Linux Clang)

on:
  push:
    branches:
      - "**"
    paths:
      - ".github/workflows/testmt1993732-with-clang.yml"
      - "src/**"
      - "test/**"
      - "**/CMakeLists.txt"
      - "**.cmake"

  pull_request:
    branches:
      - "**"
    paths:
      - ".github/workflows/testmt1993732-with-clang.yml"
      - "src/**"
      - "test/**"
      - "**/CMakeLists.txt"
      - "**.cmake"

permissions:
  contents: read

jobs:
  testmt1993732-with-clang:
    name: TestMT1993732 (AMD64 GNU/Linux Clang)
    timeout-minutes: 180
    runs-on: ubuntu-latest
    strategy:
      matrix:
        image:
          - url: ghcr.io/zhao-shihan/rgb-docker:mpich
          - url: ghcr.io/zhao-shihan/rgb-docker:openmpi
    container: ${{ matrix.image.url }}
    defaults:
      run:
        shell: bash
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Build
        run: |
          . /environment
          mkdir build && cd build
          cmake -G Ninja .. \
            -DCMAKE_C_COMPILER=clang \
            -DCMAKE_C_FLAGS='-march=native' \
            -DCMAKE_CXX_COMPILER=clang++ \
            -DCMAKE_CXX_FLAGS='-march=native' \
            -DBUILD_TESTING=OFF \
            -DMUSTARD_BUILD_TESTING=ON \
            -DMUSTARD_FULL_UNITY_BUILD=ON
          ninja

      - name: Run TestMT1993732
        run: |
          . /environment
          cd build/test
          ./MustardTest TestMT1993732
