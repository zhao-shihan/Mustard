# NOTE: This file is auto-generated by tooling/generate-test-scripts.py

name: TestCLHEPXMT1993764 (AMD64 GNU/Linux GCC)

on:
  push:
    branches:
      - "**"
    paths:
      - ".github/workflows/testclhepxmt1993764-with-gcc.yml"
      - "src/**"
      - "test/**"
      - "**/CMakeLists.txt"
      - "**.cmake"

  pull_request:
    branches:
      - "**"
    paths:
      - ".github/workflows/testclhepxmt1993764-with-gcc.yml"
      - "src/**"
      - "test/**"
      - "**/CMakeLists.txt"
      - "**.cmake"

permissions:
  contents: read

jobs:
  testclhepxmt1993764-with-gcc:
    name: TestCLHEPXMT1993764 (AMD64 GNU/Linux GCC)
    timeout-minutes: 180
    runs-on: ubuntu-latest
    strategy:
      matrix:
        image:
          - url: ghcr.io/zhao-shihan/rgb-docker:mpich
          - url: ghcr.io/zhao-shihan/rgb-docker:openmpi
    container: ${{ matrix.image.url }}
    defaults:
      run:
        shell: bash
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Build
        run: |
          . /environment
          mkdir build && cd build
          cmake -G Ninja .. \
            -DCMAKE_C_COMPILER=gcc \
            -DCMAKE_C_FLAGS='-march=native' \
            -DCMAKE_CXX_COMPILER=g++ \
            -DCMAKE_CXX_FLAGS='-march=native' \
            -DBUILD_TESTING=OFF \
            -DMUSTARD_BUILD_TESTING=ON \
            -DMUSTARD_FULL_UNITY_BUILD=ON
          ninja

      - name: Run TestCLHEPXMT1993764
        run: |
          . /environment
          cd build/test
          ./MustardTest TestCLHEPXMT1993764
