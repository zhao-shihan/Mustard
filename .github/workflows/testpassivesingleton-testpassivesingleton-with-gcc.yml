# NOTE: This file is auto-generated by tooling/generate-test-scripts.py

name: TestPassiveSingleton/TestPassiveSingleton (AMD64 GNU/Linux GCC)

on:
  push:
    branches:
      - "**"
    paths:
      - ".github/workflows/testpassivesingleton-testpassivesingleton-with-gcc.yml"
      - "src/**"
      - "test/**"
      - "**/CMakeLists.txt"
      - "**.cmake"

  pull_request:
    branches:
      - "**"
    paths:
      - ".github/workflows/testpassivesingleton-testpassivesingleton-with-gcc.yml"
      - "src/**"
      - "test/**"
      - "**/CMakeLists.txt"
      - "**.cmake"

permissions:
  contents: read

jobs:
  testpassivesingleton-testpassivesingleton-with-gcc:
    name: TestPassiveSingleton/TestPassiveSingleton (AMD64 GNU/Linux GCC)
    timeout-minutes: 180
    runs-on: ubuntu-latest
    strategy:
      matrix:
        image:
          - url: ghcr.io/zhao-shihan/rgb-docker:mpich
          - url: ghcr.io/zhao-shihan/rgb-docker:openmpi
    container: ${{ matrix.image.url }}
    defaults:
      run:
        shell: bash
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Build
        run: |
          . /environment
          mkdir build && cd build
          cmake -G Ninja .. \
            -DCMAKE_C_COMPILER=gcc \
            -DCMAKE_C_FLAGS='-march=native' \
            -DCMAKE_CXX_COMPILER=g++ \
            -DCMAKE_CXX_FLAGS='-march=native' \
            -DBUILD_TESTING=OFF \
            -DMUSTARD_BUILD_TESTING=ON \
            -DMUSTARD_FULL_UNITY_BUILD=ON
          ninja

      - name: Run TestPassiveSingleton/TestPassiveSingleton
        run: |
          . /environment
          source tooling/parexec.sh
          cd build/test
          ./MustardTest TestPassiveSingleton
          parexec --use-hwthreads ./MustardTest TestPassiveSingleton
